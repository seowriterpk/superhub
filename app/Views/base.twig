<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title }}</title>
    <meta name="description" content="{{ meta_description }}">
    <meta name="csrf-token" content="{{ csrf_token }}">
    
    <!-- SEO Meta -->
    <link rel="canonical" href="{{ canonical_url }}">
    <meta property="og:title" content="{{ page_title }}">
    <meta property="og:description" content="{{ meta_description }}">
    <meta property="og:url" content="{{ canonical_url }}">
    <meta property="og:site_name" content="{{ site_settings.site_name }}">
    <meta property="og:type" content="website">
    {% if og_image %}
    <meta property="og:image" content="{{ og_image }}">
    {% endif %}
    
    <!-- Stylesheets -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/assets/css/app.css">
    
    {% if structured_data %}
    <script type="application/ld+json">{{ structured_data|json_encode|raw }}</script>
    {% endif %}
    
    {% if breadcrumbs %}
    <script type="application/ld+json">{{ breadcrumbs|json_encode|raw }}</script>
    {% endif %}
</head>
<body class="bg-gray-900 text-white min-h-screen">
    <!-- Navigation -->
    <nav class="bg-gray-800 border-b border-gray-700">
        <div class="container mx-auto px-4">
            <div class="flex items-center justify-between h-16">
                <div class="flex items-center">
                    <a href="/" class="text-2xl font-bold text-red-500">{{ site_settings.site_name }}</a>
                </div>
                
                <div class="hidden md:flex items-center space-x-6">
                    <a href="/" class="hover:text-red-400 transition-colors">Home</a>
                    <a href="/trending" class="hover:text-red-400 transition-colors">Trending</a>
                    <a href="/categories" class="hover:text-red-400 transition-colors">Categories</a>
                    <a href="/contact" class="hover:text-red-400 transition-colors">Contact</a>
                </div>
                
                <!-- Search -->
                <div class="flex items-center">
                    <form action="/search" method="GET" class="relative">
                        <input type="text" name="query" placeholder="Search videos..." 
                               class="bg-gray-700 text-white px-4 py-2 pr-10 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500">
                        <button type="submit" class="absolute right-2 top-2 text-gray-400 hover:text-white">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                            </svg>
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main>
        {% block content %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 border-t border-gray-700 mt-16">
        <div class="container mx-auto px-4 py-8">
            <div class="grid md:grid-cols-4 gap-8">
                <div>
                    <h3 class="text-lg font-bold mb-4 text-red-500">{{ site_settings.site_name }}</h3>
                    <p class="text-gray-400 text-sm">{{ site_settings.site_description }}</p>
                </div>
                
                <div>
                    <h4 class="font-semibold mb-4">Categories</h4>
                    <ul class="space-y-2 text-sm text-gray-400">
                        <li><a href="/categories" class="hover:text-white">All Categories</a></li>
                        <li><a href="/trending" class="hover:text-white">Trending</a></li>
                    </ul>
                </div>
                
                <div>
                    <h4 class="font-semibold mb-4">Legal</h4>
                    <ul class="space-y-2 text-sm text-gray-400">
                        <li><a href="/privacy-policy" class="hover:text-white">Privacy Policy</a></li>
                        <li><a href="/dmca" class="hover:text-white">DMCA</a></li>
                        <li><a href="/2257" class="hover:text-white">18 U.S.C. 2257</a></li>
                        <li><a href="/contact" class="hover:text-white">Contact</a></li>
                    </ul>
                </div>
                
                <div>
                    <h4 class="font-semibold mb-4">Contact</h4>
                    <p class="text-sm text-gray-400">
                        Email: <a href="mailto:{{ site_settings.contact_email }}" class="hover:text-white">{{ site_settings.contact_email }}</a>
                    </p>
                </div>
            </div>
            
            <div class="border-t border-gray-700 mt-8 pt-8 text-center text-sm text-gray-400">
                <p>&copy; {{ "now"|date("Y") }} {{ site_settings.site_name }}. All rights reserved.</p>
                <p class="mt-2">Warning: This site contains adult content. All models are 18+ years old.</p>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="/assets/js/app.js"></script>
</body>
</html>